diff --git a/src/agentscope/app/_router/_schema/_model.py b/src/agentscope/app/_router/_schema/_model.py
--- a/src/agentscope/app/_router/_schema/_model.py
+++ b/src/agentscope/app/_router/_schema/_model.py
@@ -20,3 +20,8 @@ class ListModelsRequest(BaseModel):
     provider: str = Field(
         description="The provider type, e.g. openai, dashscope, etc.",
     )
+    credential_id: str | None = Field(
+        default=None,
+        description=("Optional credential id for credential-specific filtering."),
+    )
diff --git a/src/agentscope/app/_router/_schema/_tts_model.py b/src/agentscope/app/_router/_schema/_tts_model.py
--- a/src/agentscope/app/_router/_schema/_tts_model.py
+++ b/src/agentscope/app/_router/_schema/_tts_model.py
@@ -22,3 +22,8 @@ class ListTTSModelsRequest(BaseModel):
     provider: str = Field(
         description="The provider type, e.g. dashscope_credential.",
     )
+    credential_id: str | None = Field(
+        default=None,
+        description=("Optional credential id for credential-specific filtering."),
+    )
diff --git a/src/agentscope/app/_router/_schema/_embedding_model.py b/src/agentscope/app/_router/_schema/_embedding_model.py
--- a/src/agentscope/app/_router/_schema/_embedding_model.py
+++ b/src/agentscope/app/_router/_schema/_embedding_model.py
@@ -22,3 +22,8 @@ class ListEmbeddingModelsRequest(BaseModel):
     provider: str = Field(
         description="The provider type, e.g. openai, dashscope, etc.",
     )
+    credential_id: str | None = Field(
+        default=None,
+        description=("Optional credential id for credential-specific filtering."),
+    )
diff --git a/src/agentscope/app/_router/_model.py b/src/agentscope/app/_router/_model.py
--- a/src/agentscope/app/_router/_model.py
+++ b/src/agentscope/app/_router/_model.py
@@ -4,8 +4,14 @@ from fastapi import APIRouter, Depends, HTTPException, status

 from ._schema import ListModelsResponse, ListModelsRequest
+from ..access import ResourceKind
+from ..deps import get_current_user_id, get_resource_access_service
+from .._service import ResourceAccessService
 from ...credential import CredentialFactory
@@ -20,6 +26,8 @@ async def list_models(
 async def list_models(
     body: ListModelsRequest = Depends(),
+    user_id: str = Depends(get_current_user_id),
+    access: ResourceAccessService = Depends(get_resource_access_service),
 ) -> ListModelsResponse:
@@ -37,4 +45,22 @@ async def list_models(
         )

     models = credential_cls.get_chat_model_class().list_models()
+    if body.credential_id:
+        credential = await access.get_resource(
+            user_id,
+            ResourceKind.CREDENTIAL,
+            body.credential_id,
+        )
+        if credential.data.get("type") != body.provider:
+            raise HTTPException(
+                status_code=status.HTTP_400_BAD_REQUEST,
+                detail="Credential type does not match the requested provider.",
+            )
+        if credential.data.get("name") == "Volcengine Ark":
+            models = [
+                model
+                for model in models
+                if model.label == "Volcengine Ark (Keychain)"
+            ]
     return ListModelsResponse(models=models, total=len(models))
diff --git a/src/agentscope/app/_router/_tts_model.py b/src/agentscope/app/_router/_tts_model.py
--- a/src/agentscope/app/_router/_tts_model.py
+++ b/src/agentscope/app/_router/_tts_model.py
@@ -4,8 +4,14 @@ from fastapi import APIRouter, Depends, HTTPException, status

 from ._schema import ListTTSModelsResponse, ListTTSModelsRequest
+from ..access import ResourceKind
+from ..deps import get_current_user_id, get_resource_access_service
+from .._service import ResourceAccessService
 from ...credential import CredentialFactory
@@ -20,6 +26,8 @@ async def list_tts_models(
 async def list_tts_models(
     body: ListTTSModelsRequest = Depends(),
+    user_id: str = Depends(get_current_user_id),
+    access: ResourceAccessService = Depends(get_resource_access_service),
 ) -> ListTTSModelsResponse:
@@ -37,4 +45,20 @@ async def list_tts_models(
         )

     models = credential_cls.list_tts_models()
+    if body.credential_id:
+        credential = await access.get_resource(
+            user_id,
+            ResourceKind.CREDENTIAL,
+            body.credential_id,
+        )
+        if credential.data.get("type") != body.provider:
+            raise HTTPException(
+                status_code=status.HTTP_400_BAD_REQUEST,
+                detail="Credential type does not match the requested provider.",
+            )
+        if credential.data.get("name") == "Volcengine Ark":
+            models = []
     return ListTTSModelsResponse(models=models, total=len(models))
diff --git a/src/agentscope/app/_router/_embedding_model.py b/src/agentscope/app/_router/_embedding_model.py
--- a/src/agentscope/app/_router/_embedding_model.py
+++ b/src/agentscope/app/_router/_embedding_model.py
@@ -4,8 +4,14 @@ from fastapi import APIRouter, Depends, HTTPException, status

 from ._schema import ListEmbeddingModelsResponse, ListEmbeddingModelsRequest
+from ..access import ResourceKind
+from ..deps import get_current_user_id, get_resource_access_service
+from .._service import ResourceAccessService
 from ...credential import CredentialFactory
@@ -23,6 +29,8 @@ async def list_embedding_models(
 async def list_embedding_models(
     body: ListEmbeddingModelsRequest = Depends(),
+    user_id: str = Depends(get_current_user_id),
+    access: ResourceAccessService = Depends(get_resource_access_service),
 ) -> ListEmbeddingModelsResponse:
@@ -49,4 +57,20 @@ async def list_embedding_models(
     # rather than 404 — "none available" is a valid answer here.
     models = [] if embedding_cls is None else embedding_cls.list_models()
+    if body.credential_id:
+        credential = await access.get_resource(
+            user_id,
+            ResourceKind.CREDENTIAL,
+            body.credential_id,
+        )
+        if credential.data.get("type") != body.provider:
+            raise HTTPException(
+                status_code=status.HTTP_400_BAD_REQUEST,
+                detail="Credential type does not match the requested provider.",
+            )
+        if credential.data.get("name") == "Volcengine Ark":
+            models = []
     return ListEmbeddingModelsResponse(models=models, total=len(models))
diff --git a/src/agentscope/app/_router/_knowledge_base.py b/src/agentscope/app/_router/_knowledge_base.py
--- a/src/agentscope/app/_router/_knowledge_base.py
+++ b/src/agentscope/app/_router/_knowledge_base.py
@@ -105,6 +105,8 @@ async def list_kb_embedding_models(
     for credential in credentials:
         credential_type = credential.data.get("type")
         if not credential_type:
             continue
+        if credential.data.get("name") == "Volcengine Ark":
+            continue
         credential_cls = CredentialFactory.get_credential_class(
diff --git a/examples/web_ui/frontend/src/api/model.ts b/examples/web_ui/frontend/src/api/model.ts
--- a/examples/web_ui/frontend/src/api/model.ts
+++ b/examples/web_ui/frontend/src/api/model.ts
@@ -2,14 +2,23 @@ import { client } from './client';
 import type { ListEmbeddingModelResponse, ListModelResponse, ListTTSModelResponse } from './types';

 export const modelApi = {
-	list: (provider: string) => client.get<ListModelResponse>('/model/', { provider }),
+	list: (provider: string, credentialId?: string) =>
+		client.get<ListModelResponse>('/model/', {
+			provider,
+			...(credentialId ? { credential_id: credentialId } : {}),
+		}),
 };

 export const ttsModelApi = {
-	list: (provider: string) => client.get<ListTTSModelResponse>('/tts-model/', { provider }),
+	list: (provider: string, credentialId?: string) =>
+		client.get<ListTTSModelResponse>('/tts-model/', {
+			provider,
+			...(credentialId ? { credential_id: credentialId } : {}),
+		}),
 };

 export const embeddingModelApi = {
-	list: (provider: string) =>
-		client.get<ListEmbeddingModelResponse>('/embedding-model/', { provider }),
+	list: (provider: string, credentialId?: string) =>
+		client.get<ListEmbeddingModelResponse>('/embedding-model/', {
+			provider,
+			...(credentialId ? { credential_id: credentialId } : {}),
+		}),
 };
diff --git a/examples/web_ui/frontend/src/pages/credential/index.tsx b/examples/web_ui/frontend/src/pages/credential/index.tsx
--- a/examples/web_ui/frontend/src/pages/credential/index.tsx
+++ b/examples/web_ui/frontend/src/pages/credential/index.tsx
@@ -263,17 +263,17 @@ function DetailPanel({ credential, schema, onEdit, onDelete }: DetailPanelProps) {
 		Promise.all([
 			modelApi
-				.list(type)
+				.list(type, credential.id)
 				.then((res) => res.models)
 				.catch(() => [] as ModelCard[]),
 			ttsModelApi
-				.list(type)
+				.list(type, credential.id)
 				.then((res) => res.models)
 				.catch(() => [] as TTSModelCard[]),
 			embeddingModelApi
-				.list(type)
+				.list(type, credential.id)
 				.then((res) => res.models)
 				.catch(() => [] as EmbeddingModelCard[]),
diff --git a/examples/web_ui/frontend/src/hooks/useAvailableModels.ts b/examples/web_ui/frontend/src/hooks/useAvailableModels.ts
--- a/examples/web_ui/frontend/src/hooks/useAvailableModels.ts
+++ b/examples/web_ui/frontend/src/hooks/useAvailableModels.ts
@@ -29,7 +29,7 @@ export function useAvailableModels() {
 					if (!type) return;
 					if (!result[type]) result[type] = [];
 					try {
-						const { models } = await modelApi.list(type);
+						const { models } = await modelApi.list(type, credential.id);
 						result[type].push({ credential, models });
diff --git a/examples/web_ui/frontend/src/hooks/useAvailableTTSModels.ts b/examples/web_ui/frontend/src/hooks/useAvailableTTSModels.ts
--- a/examples/web_ui/frontend/src/hooks/useAvailableTTSModels.ts
+++ b/examples/web_ui/frontend/src/hooks/useAvailableTTSModels.ts
@@ -31,7 +31,7 @@ export function useAvailableTTSModels() {
 					if (!type) return;
 					if (!result[type]) result[type] = [];
 					try {
-						const { models } = await ttsModelApi.list(type);
+						const { models } = await ttsModelApi.list(type, credential.id);
 						if (models.length > 0) {
diff --git a/src/agentscope/app/_router/_session.py b/src/agentscope/app/_router/_session.py
--- a/src/agentscope/app/_router/_session.py
+++ b/src/agentscope/app/_router/_session.py
@@ -48,6 +48,7 @@
     StorageBase,
     TeamRecord,
 )
+from ...credential import CredentialFactory
 from ...message import ToolCallState
 from ..storage._utils import _ensure_team_members
 from ...event import CustomEvent
@@ -148,11 +149,40 @@
     if config is None:
         return
     # ``get_resource`` raises 404 when the credential is neither owned
     # nor shared to the viewer — exactly the semantics we want.
-    await access.get_resource(
+    credential = await access.get_resource(
         user_id,
         ResourceKind.CREDENTIAL,
         config.credential_id,
     )
+    if credential.data.get("name") != "Volcengine Ark":
+        return
+    if isinstance(config, TTSModelConfig):
+        raise HTTPException(
+            status_code=status.HTTP_400_BAD_REQUEST,
+            detail=(
+                "Volcengine Ark has no validated TTS model in this "
+                "environment."
+            ),
+        )
+    credential_cls = CredentialFactory.get_credential_class(config.type)
+    if credential_cls is None:
+        raise HTTPException(
+            status_code=status.HTTP_400_BAD_REQUEST,
+            detail="Credential provider is not supported.",
+        )
+    validated_models = {
+        card.name
+        for card in credential_cls.get_chat_model_class().list_models()
+        if card.label == "Volcengine Ark (Keychain)"
+    }
+    if config.model not in validated_models:
+        raise HTTPException(
+            status_code=status.HTTP_400_BAD_REQUEST,
+            detail=(
+                "The requested Volcengine Ark chat model has not passed "
+                "the environment validation gate."
+            ),
+        )


 async def _ensure_knowledge_bases_exist(
diff --git a/src/agentscope/app/_service/_knowledge_base.py b/src/agentscope/app/_service/_knowledge_base.py
--- a/src/agentscope/app/_service/_knowledge_base.py
+++ b/src/agentscope/app/_service/_knowledge_base.py
@@ -129,6 +129,18 @@
                 ``409`` when the requested embedding dimension
                 violates the manager's dimension policy.
         """
+        credential = await self._access.resolve_credential(
+            user_id,
+            embedding_model_config.credential_id,
+        )
+        if credential.data.get("name") == "Volcengine Ark":
+            raise HTTPException(
+                status_code=status.HTTP_400_BAD_REQUEST,
+                detail=(
+                    "Volcengine Ark has no validated embedding model in "
+                    "this environment."
+                ),
+            )
         try:
             return await self._manager.create_knowledge_base(
                 user_id=user_id,
