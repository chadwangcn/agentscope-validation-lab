server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;

    location = / {
        return 302 /agentscope/;
    }

    location /agentscope/ {
        try_files $uri $uri/ /agentscope/index.html;
    }
}
